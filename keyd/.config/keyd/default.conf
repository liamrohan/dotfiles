[ids]
*

[main]
# tap Caps = Esc, hold Caps = Control
capslock = overload(control, esc)
# (optional) keep Esc if you want it as Caps:
esc = capslock

# IMPORTANT: do NOT set `control = oneshot(control)` if you want to hold Ctrl
# (comment it out if it’s in your file)

[control]
h = left
j = down
k = up
l = right
[ = esc
